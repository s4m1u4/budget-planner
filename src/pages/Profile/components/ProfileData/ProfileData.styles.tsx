import styled from "@emotion/styled";

export const UserData = styled.p({
  fontSize: "18px",
  marginBottom: "10px",
});

export const UserDataMain = styled.span({
  fontSize: "inherit",
  fontWeight: "700",
});
